<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.jun.meeting.Dao.ContactDao">
    <select id="contactInsert" parameterType="HashMap">
        INSERT INTO contact (contactWriter, contactEmail, contactPhoneNumber, contactSubject, contactMessage, contactID)
        VALUES (#{name},#{requestEmail},#{phone},#{subject},#{message},#{userEmail});
    </select>
</mapper>
