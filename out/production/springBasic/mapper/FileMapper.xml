<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.jun.meeting.Dao.FileDao">
    <select id="insertFileToDB" parameterType="HashMap">
        INSERT INTO user_photo (fileName, fileOriginalName, filePath, fileSize, userEmail)
        VALUES (#{file_name},#{file_orig},#{file_path},#{file_size},#{userEmail});
    </select>
</mapper>
