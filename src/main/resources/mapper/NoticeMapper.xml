<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jun.board.Dao.NoticeDao">
	<select id="noticeList" resultType="HashMap">
    	SELECT noticeInfo.*, userInfo.USER_NAME
		FROM NOTICE_INFORMATION noticeInfo, USER_INFORMATION userInfo
		WHERE noticeInfo.NOTICE_WRITER = userInfo.USER_EMAIL
    </select>
</mapper>